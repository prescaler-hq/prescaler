

# kontxt — ephemeral files (MD files are safe to commit)
.kontxt/*.db
.kontxt/*.pid
.kontxt/*.sock
.vscode/extensions.json

# Local-only repository process notes; never publish these files.
.local/
